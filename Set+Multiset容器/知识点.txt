基于二叉树的容器会自动排序，且查找效率极好

二叉搜索树：左边比右边小
平衡二叉树：为了保证搜索效率，尽量让左右深度相差不超过1

红黑树：平衡二叉树的一种

07 set/multiset集合容器   属于关联式容器
基于红黑树实现，因此最大的特性为根据值进行自动排序。（所以在插入时就必须要保证能排序，如果插入对象，就要加入排序规则！）

只提供insert()方法，没有push和pop
提供迭代器，但不允许通过迭代器改变值，保证位置不会乱

set和multiset的唯一区别：
set不允许放重复的元素，multiset允许